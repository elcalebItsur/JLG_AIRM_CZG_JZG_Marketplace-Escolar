# ITSUR Marketplace — FASE 1

Resumen de entregables de FASE 1:

- Arquitectura propuesta (mono-repo Expo, capas limpias).
- Estructura de carpetas base en `src/`.
- Tipos TypeScript para `User`, `Product` y `Role`.
- Validadores de dominio institucional (`@alumnos.itsur.edu.mx`, `@itsur.edu.mx`).
- Servicio `authService` con lógica de validación del dominio (placeholders para integración con backend).
- Instrucciones para crear admin de forma segura.

Siguientes pasos:

- Revisar los archivos en `src/` y confirmar para FASE 2 (creación del proyecto Expo + TypeScript).
- Si quieres que haga el push, me das acceso (PAT) o lo haces localmente con las instrucciones abajo.

Instrucciones para subir la rama creada localmente:

```bash
cd "c:/Users/messi/OneDrive/Escritorio/ISC/Semestre 8/Taller de Investigación II/mktpesc/JLG_AIRM_CZG_JZG_Marketplace-Escolar"
git push origin feature/phase-1
```

